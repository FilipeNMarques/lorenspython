#file = open('arquivo.txt', 'w+')
##with open('carteira.txt', 'w+') as arquivo:
 #    for item in carteira:
  #       arquivo.write(str(carteira))
#with open('carteira.txt', 'r+', encoding='utf-8') as f:
    #resultado = f.read()
    #format_resul = list(resultado)
    #format_resul.append(criptos)
    #format_carteira = str(format_resul)
    #f.write(format_carteira)

#with open('carteira.txt', 'w+') as f:
   # f.write()

